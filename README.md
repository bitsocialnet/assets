# Bitsocial Assets

Official Bitsocial visual assets.

## License

Unless otherwise noted, the assets in this repository are licensed under the GNU General Public License v3.0 or later (`GPL-3.0-or-later`). See [LICENSE](LICENSE).

The Bitsocial name and logo may also function as project marks. The copyright license permits copying, modification, and redistribution of the asset files; it does not grant permission to misrepresent affiliation, endorsement, or ownership.

## Files

All distributable files live in [assets](assets).

### Logo

- `logo.svg` is the full SVG logo export.
- `logo-32.svg` is the 32 x 32 SVG icon export.
- `logo.ai` is the Adobe Illustrator source export.
- `logo.eps` is the EPS source export.

PNG logo exports are provided at `32`, `64`, `96`, `180`, `512`, `1024`, and `3000` pixels:

- `logo-transparent-*.png` has a transparent background.
- `logo-on-white-*.png` has a white background.
- `logo-on-black-*.png` has a black background.

### Banners

- `banner-wide-dark.*`
- `banner-wide-light.*`
- `banner-compact-dark.*`
- `banner-compact-light.*`

The `banner-*` files are the reusable hero/banner assets currently used by the Bitsocial web surfaces.

### pkc-js

Wordmark-over-p2p-mesh assets for the [pkc-js](https://github.com/bitsocialnet/pkc-js) repository, in [assets/pkc-js](assets/pkc-js):

- `icon-on-white-500.png` and `icon-on-black-500.png` are 500 x 500 square icons (e.g. group/community icons).
- `banner-light-1280x640.png` and `banner-dark-1280x640.png` are 1280 x 640 banners sized for the GitHub repository social preview.

## Export Notes

Logo PNGs and `logo-32.svg` were generated from `assets/logo.svg`.

Recommended defaults:

- Use `assets/logo-32.svg` for a 32 x 32 SVG icon request.
- Use `assets/logo-transparent-512.png` for general app icon previews.
- Use `assets/logo-transparent-3000.png` when a high-resolution raster source is needed.
